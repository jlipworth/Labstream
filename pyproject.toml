[project]
name = "labstream-tooling"
version = "0.1.0"
description = "Local Python tooling for Labstream profiling and repository checks."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = []
