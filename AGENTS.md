# Labstream — agent notes (Codex)

Read `CLAUDE.md` for the canonical project guidance, build/test loops, cleanup rules,
and hard constraints. Keep this file as a pointer only so agent instructions do not drift.
