# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
DerivedData/
.build-dd/
build/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# Secrets — never commit Plex tokens / client identifiers
scripts/ci-hygiene.local
*.token
secrets.plist
Secrets.swift
.env
# Live PMS integration-probe creds (real token + hostname) — see *.example
scripts/plex-live.env
# Local-only extra forbidden-string list for ci-hygiene.sh (holds the real host/IP) — never commit
scripts/ci-hygiene.local
# cloc scratch output
cloc-ignored.txt

# Per-developer signing (Apple Developer Team ID) — never commit
Signing.local.xcconfig
*.local.xcconfig

# CocoaPods / Carthage (unlikely, but just in case)
Pods/
Carthage/Build/

# Claude Code session data + agent worktrees (never commit) — but DO version skills
.claude/*
!.claude/skills/

# Superpowers visual brainstorming scratch data (never commit)
.superpowers/

# Python tooling
.venv/
__pycache__/
*.py[cod]
