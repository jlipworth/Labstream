# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
DerivedData/
build/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved

# Secrets — never commit Plex tokens / client identifiers
*.token
secrets.plist
Secrets.swift
.env

# Per-developer signing (Apple Developer Team ID) — never commit
Signing.local.xcconfig
*.local.xcconfig

# CocoaPods / Carthage (unlikely, but just in case)
Pods/
Carthage/Build/

# Claude Code session data + agent worktrees (never commit)
.claude/
